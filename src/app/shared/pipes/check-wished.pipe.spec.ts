import { CheckWishedPipe } from './check-wished.pipe';

describe('CheckWishedPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckWishedPipe();
    expect(pipe).toBeTruthy();
  });
});
